
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: myDB
      username: root
      password: root
      authentication-database: admin

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:9002/eureka

management:
  tracing:
    sampling:
      probability: 1.0
